# 机のいらない救急英語 🚑

救急隊向けの「英語問診トレーナー」アプリです。Duolingo のような感覚で、
英語の医療問診を声に出して練習し、発音を自動判定します。

## これは何？

- 🎤 **発音トレーニング** — 英語の問診を音読 → AIが聞き取って採点
- 🔀 **分岐シナリオ** — 患者の返答を選ぶと次の質問が変わる
- 🔤 **単語クイズ** — 救急医療の英単語を4択で学習
- 🪜 **レベル進行** — Lv1〜10、クリアで次が解放
- 🔥 **記録機能** — ストリーク・XP・バッジ・学習カレンダー

すべてブラウザ内（オフライン）で動作し、学習記録は端末に保存されます。

## 使い方

### そのまま開く
`index.html` をブラウザ（**Chrome 推奨**）で開くだけで動きます。
マイクの発音判定を使うには Chrome が必要です。

### ファイル構成
| ファイル | 役割 |
|---------|------|
| `index.html` | 画面とデザイン（HTML / CSS） |
| `ems-app.js` | アプリの動作ロジック |
| `ems-data.js` | シナリオ・単語などのデータ |

## ネットで公開する

### 方法1：Cloudflare Pages（推奨・無料・高速）

1. [Cloudflare](https://dash.cloudflare.com) のアカウントを作成
2. **Workers & Pages → Pages → GitHub に接続**
3. このリポジトリを選択、プロジェクト名 `ems-english-trainer` で作成
4. デプロイ完了後、GitHub Actions で自動デプロイが有効になります
5. （オプション）独自ドメイン取得・設定：**Custom domains** から追加

以降、`main` ブランチへ push するたびに、自動的に Cloudflare Pages にデプロイされます。

### 方法2：GitHub Pages

このリポジトリの **Settings → Pages** で、ブランチ `main` / フォルダ `/ (root)`
を選んで保存すると、`https://<ユーザー名>.github.io/ems-english-trainer/` で公開されます。

## 開発を続けるには

このリポジトリを Claude Code で開いて、
「シナリオを追加して」「色を変えて」のように頼むと編集してくれます。
区切りごとに変更を commit / push して保存してください。
